<% if logged_in? %>
<h3><%= @current_user.username %> | <%= link_to "Logout", sessions_path, method: :delete %> </h3>
<% else %>
<h3>You are not logged in! <%= link_to "Login", login_path %> or <%= link_to "Sign Up", new_user_path %></h3>
<% end %>
